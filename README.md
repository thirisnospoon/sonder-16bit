# Sonder

Мини-соцсеть с современным вебом и намеренно нелепым бэкендом.

**Бизнес-логика исполняется шестнадцатибитным кодом под DOS.** React,
PostgreSQL и Java занимаются вводом-выводом: принять запрос, достать данные,
сохранить результат, разослать события. Решения — можно ли создать пост,
свободен ли ник, вправе ли модератор — принимает Turbo Pascal 7.

```
React ──REST──► Java ──SOAP──► Turbo Pascal    решение
                 │                  │
                 └──── JDBC ────────┴──► PostgreSQL ──NOTIFY──► события
```

Форма — функциональное ядро и императивная оболочка. Ядро детерминировано и не
делает ввода-вывода не из эстетики, а потому что под DOS его сделать нечем: ни
драйвера PostgreSQL, ни сетевого стека. Под Pascal написан собственный
ООП-фреймворк с кооперативной многозадачностью: линия медленная, и пока уходит
один ответ, следующая команда уже принимается.

> `Sonder` — рабочее имя продукта, `NODE-7` — кодовое имя DOS-ноды,
> `TurboCore` — имя Pascal-фреймворка. Меняются в одном месте.

## Главный принцип

**Ни одна поверхность не выглядит старой.** Веб — современный продуктовый
дизайн. У DOS-ноды поверхности нет вовсе: это ядро, а не клиент. Если не
заглядывать под капот, догадаться о содержимом невозможно. Абсурд живёт в
архитектуре, а не в оформлении.

## Второй принцип

**Каждая нелепость несёт нагрузку.** Ни одна древняя технология не стоит в
проекте ради вида: SOAP — это контракт, из которого генерируются стабы для трёх
языков; CORBA — единственный синхронный канал между двумя Java-сервисами;
кооперативные файберы — единственный способ конвейеризовать работу поверх
линии в 11.5 КБ/с.

Проверка простая: если технологию убрать и ничего не сломается, она уходит.
Так из проекта уже вылетели EMS и XSLT-отрисовка — измерения показали, что они
не нужны.

## Документы

| Файл | О чём |
|---|---|
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) | Система целиком: границы, контракты, потоки данных, модель ошибок |
| [docs/TURBOCORE.md](docs/TURBOCORE.md) | Дизайн Pascal-фреймворка: файберы, планировщик, память, протокольный стек |
| [docs/PLAN.md](docs/PLAN.md) | Фазы, гейты, критерии готовности, что можно параллелить |
| [docs/ENGINEERING.md](docs/ENGINEERING.md) | Инженерные стандарты: определение готовности, гейты CI, процесс ADR |
| [docs/PHASE0-FINDINGS.md](docs/PHASE0-FINDINGS.md) | Журнал находок фазы 0: восемь находок и три пройденных спайка |
| [docs/RISKS.md](docs/RISKS.md) | Реестр рисков с планами отхода |
| [docs/adr/](docs/adr/) | Зафиксированные архитектурные решения |

## Статус

Фаза 0, в работе. Пройдены три спайка из пяти:

| Спайк | Результат |
|---|---|
| S1 · Файберы | 100 000 переключений контекста, глубина стека 38–224 Б |
| S1b · Модель памяти | `large`, 599 628 Б дальней кучи, без утечек |
| S2 · Транспорт | 11 503 Б/с на направление, 13 мс фиксированных на круг |

Образ тулчейна собирает кросс-компилятор FPC `i8086-msdos`, RTL под все шесть
моделей памяти и гоняет DOS-бинарники под DOSBox. Прикладного кода пока нет.
