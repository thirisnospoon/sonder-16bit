import { defineConfig, devices } from '@playwright/test'

/*
 * Сценарии идут против ПОДНЯТОЙ системы, а не против заглушки.
 *
 * За адресом стоит весь состав: nginx, оболочка на Java, Firebird и
 * шестнадцатибитная NODE-7 под DOSBox. Подменить любое звено значило бы
 * проверять не то: заглушка отвечает так, как её научили, а система — так,
 * как она устроена.
 *
 * Отсюда и сроки. Команда идёт через нульмодем на 11 503 Б/с, событие
 * доезжает до проекции дренажем раз в секунду: ожидания здесь секундные,
 * и это не медлительность браузера, а физика линии.
 */
export default defineConfig({
  testDir: '.',
  // Строго последовательно: система одна, и параллельные сценарии
  // мешали бы друг другу состоянием базы.
  workers: 1,
  fullyParallel: false,
  // Повторов нет. Проверка, которая проходит со второго раза,
  // сообщает о системе ровно то же, что и падающая: она ненадёжна.
  retries: 0,
  timeout: 60_000,
  expect: { timeout: 15_000 },
  reporter: [['list']],
  use: {
    baseURL: process.env['SONDER_URL'] ?? 'http://127.0.0.1:8080',
    // Скриншот и трасса только на падении: на зелёном прогоне это
    // мегабайты, которые никто не откроет.
    screenshot: 'only-on-failure',
    trace: 'retain-on-failure',
    locale: 'ru-RU',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
})
