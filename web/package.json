{
  "name": "sonder-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Фронт Sonder: собственный реактивный слой на сигналах, без зависимостей времени исполнения (ADR-0012)",
  "scripts": {
    "build": "tsc --build",
    "test": "tsc --build && node --test build/test/"
  },
  "devDependencies": {
    "@types/node": "20.12.12",
    "typescript": "5.4.5"
  }
}
